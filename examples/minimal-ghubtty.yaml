# This is an example ~/.config/ghubtty/ghubtty.yaml file with only the required
# settings.

# This file does not list all of the available options.  For a full
# list with explanations, see the 'reference-ghubtty.yaml' file.

servers:
  - name: CHANGEME
    url: https://CHANGEME.example.org/
    username: CHANGEME
    # Set corresponding HTTP password in github settings
    password: CHANGEME
    git-root: ~/git/
