# This is an example ~/.config/hubtty/hubtty.yaml file with only the required
# settings.

# This file does not list all of the available options.  For a full
# list with explanations, see the 'reference-hubtty.yaml' file.

servers:
  - name: CHANGEME
    git-root: ~/git/
